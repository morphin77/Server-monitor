<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Nods <b class="caret"></b></a>
  <ul class="dropdown-menu">
    <% if current_user.nodes.present? %>
      <li><%= link_to 'All nods', nodes_path %></li>
      <li class="divider"></li>
      <% current_user.nodes.each do |node| %>
        <li><%= link_to node.name, edit_node_path(node) %></li>
      <% end %>
      <li class="divider"></li>
    <% end %>  
      <li><%= link_to 'Add New', new_node_path %></li>
  </ul>
</li>
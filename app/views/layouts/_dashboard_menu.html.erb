<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">Dashboards <b class="caret"></b></a>
  <ul class="dropdown-menu">
    <% if current_user.dashboards.present? %>
      <li><%= link_to 'All dashboards', dashboards_path %></li>
      <li class="divider"></li>
      <% current_user.dashboards.each do |dashboard| %>
        <li><%= link_to dashboard.name, dashboard %></li>
      <% end %>
      <li class="divider"></li>
    <% end %>  
      <li><%= link_to 'Add New', new_dashboard_path %></li>
  </ul>
</li>

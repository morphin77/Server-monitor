<div class="page-header">
  <div class="container">
    <h2>
     	Editing Dashboard: <%= @dashboard.name%>
      <small class="pull-right">
        <%= button_to @dashboard, {class: "btn btn-primary",:method => :get} do %>
          <span class="glyphicon glyphicon-arrow-left"></span>
        <% end %>
      </small>			
		</h2>
	</div>
</div>

<%= render 'form', dashboard: @dashboard %>

